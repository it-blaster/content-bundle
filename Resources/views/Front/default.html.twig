{% extends '::base.html.twig' %}


{% block title %}{{ page.title }} &mdash; {{ parent() }}{% endblock title %}


{% block body %}

    <h1>{{ page.title }}</h1>
    <hr>

    {{ page.content | raw }}

    <hr>
    <p>{{ page.updatedAt | date }}</p>

{% endblock body %}